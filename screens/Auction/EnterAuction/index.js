import React from 'react';
import {ScrollView, Text, View} from 'react-native';
import {styles} from './Styles';

const EnterAuction = () => {
  return (
    <ScrollView contentContainerStyle={styles.container__main}>
      <Text>enter auction</Text>
    </ScrollView>
  );
};

export default EnterAuction;
